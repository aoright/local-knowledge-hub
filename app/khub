#!/bin/sh
set -eu
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
if [ -x "$SCRIPT_DIR/.venv/bin/python3" ]; then
  exec "$SCRIPT_DIR/.venv/bin/python3" "$SCRIPT_DIR/src/knowledge_hub.py" "$@"
fi
exec /usr/bin/env python3 "$SCRIPT_DIR/src/knowledge_hub.py" "$@"
